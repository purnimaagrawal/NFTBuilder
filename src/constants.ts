export const DEFAULT_STORE_STATUS = "active";

export const DEFAULT_PUBLISH_STATUS = "unpublished";

// API paths on which we apply firebase auth middleware
export const PROTECTED_API_PATHS = ["stores", "collections", "assets"];
